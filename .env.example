# Copy to .env (gitignored) and fill in to run `npm run publish:standard --commit`.
# Create an app password at Bluesky → Settings → App Passwords. Do NOT use your login password.
BSKY_APP_PASSWORD=
# Optional; defaults to pob.dev
# BSKY_IDENTIFIER=pob.dev
