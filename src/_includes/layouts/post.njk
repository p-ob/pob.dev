---
layout: base.njk
pageType: article
---

<h1>{{ title }}</h1>
<time datetime="{{ page.date | machineDate }}">{{ page.date | readableDate }}</time>

{% if tags %}
	{% set displayTags = tags | except("post") %}
  {% if displayTags.length %}
    <div class="tags">
      {% for tag in displayTags %}
        <span class="tag">{{ tag }}</span>
      {% endfor %}
    </div>
  {% endif %}
{% endif %}

<hr/>

{{ content | safe }}
