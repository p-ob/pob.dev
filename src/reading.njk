---
excludeFromSearch: true
noSidebar: true
title: Reading
permalink: /reading/
---

<link rel="stylesheet" href="/assets/css/reading.css">

<h1>Reading</h1>
<p class="reading-description">
  Articles and posts from around the web.
  {% if readingFeedDurationLimit %}
  Showing posts from the last {{ readingFeedDurationLimit | humanizeDuration }}.
  {% endif %}
</p>

{% if readingFeedSources.length > 0 %}
<details class="reading-acknowledgements">
  <summary>Acknowledgements</summary>
  <ul class="reading-acknowledgements-list">
    {%- for source in readingFeedSources -%}
    <li>
      <a href="{{ source.siteUrl }}" target="_blank" rel="noopener noreferrer">{{ source.name }}</a>
    </li>
    {%- endfor -%}
  </ul>
</details>
{% endif %}

{% if aggregatedFeeds.length > 0 %}
<ol class="reading-items">
{%- for item in aggregatedFeeds -%}
  <li>
    <pob-tile href="{{ item.link }}" target="_blank">
      {% set itemDate = item.date | toDate %}
      <time class="reading-date" datetime="{{ item.date }}">{{ itemDate | readableDate }}</time>
      <div class="reading-details">
        <strong class="reading-title">{{ item.title }}</strong>
        <div class="reading-source">{{ item.source.name }}</div>
      </div>
    </pob-tile>
  </li>
{%- endfor -%}
</ol>
{% else %}
<p>No feed items available. Check your feeds.json configuration.</p>
{% endif %}
